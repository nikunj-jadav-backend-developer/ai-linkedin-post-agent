global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "prometheus_rules.yml"

scrape_configs:
  - job_name: 'ai-linkedin-post-agent'

    static_configs:
      - targets: ['localhost:8000']