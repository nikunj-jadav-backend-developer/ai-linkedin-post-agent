[project]
name = "backend"
version = "0.1.0"
description = ""
authors = [
    {name = "Nikunj Jadav", email = "dirensj@gmail.com"}
]
requires-python = ">=3.12,<4.0"
dependencies = ["fastapi (==0.136.3)", "langchain-core (==1.4.2)", "langchain-groq (==1.1.2)", "langgraph (==1.2.4)", "prometheus-client (==0.25.0)", "pydantic (==2.13.4)", "python-dotenv (==1.2.2)", "requests (==2.34.2)", "starlette (==1.2.1)", "uvicorn[standard] (>=0.49.0,<0.50.0)"]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"